# Documentation site toolchain. Pinned so a Pages build is reproducible and a
# theme release cannot silently change the published site. The same pins as
# aimon-core, so the two sites cannot drift apart on a theme upgrade.
#
#   pip install -r docs-requirements.txt
#   mkdocs serve          # local preview at http://127.0.0.1:8000
#   mkdocs build --strict # what CI runs
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-static-i18n==1.3.1
