// aimon-memory-core — the contract. Depends on nothing so that every other module can depend on it.
// Adding a dependency here is a three-person decision.

plugins {
    id("aimon.java-conventions")
    id("aimon.publishable")
}

dependencies { }
