/**
 * The contract layer: domain types, keys and the six SPIs.
 *
 * <p>This module has no dependencies, which is what lets three tracks build against each other's
 * stubs instead of each other's code. Changing a signature here is a decision for everyone.
 */
package at.aimon.memory.core;
